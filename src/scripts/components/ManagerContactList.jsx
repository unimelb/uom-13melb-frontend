/**
 * @jsx React.DOM
 */

'use strict';

var React = require('react/addons');
require('../../styles/ManagerContactList.css');

var ManagerContactList = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for ManagerContactList</p>
        </div>
      );
  }
});

module.exports = ManagerContactList;
